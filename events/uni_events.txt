namespace = overworld

event = {
   id = overworld.1
   hide_window = yes
   is_triggered_only = yes
   immediate = {
       # Palaven Start
       every_country = {
            every_owned_planet = {
                limit = { has_planet_flag = is_overworld_flag }
                add_modifier = { modifier = weak_magnetic_field days = -1 }
                add_modifier = { modifier = wild_storms days = -1 }
                add_modifier = { modifier = low_gravity days = -1 }
            }
        }
    }